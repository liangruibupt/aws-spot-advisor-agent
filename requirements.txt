# Core AWS dependencies
boto3>=1.34.0
botocore>=1.34.0

# AWS Bedrock AgentCore (assuming it's available via pip)
# Note: This may need to be installed differently based on AWS documentation
bedrock-agentcore
bedrock-agentcore-starter-toolkit
strands-agents
strands-agents-tools
strands-agents

# Data validation and processing
pydantic>=2.0.0
typing-extensions>=4.0.0

# Utilities
python-dotenv>=1.0.0
tenacity>=8.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
moto>=4.2.0

# Development tools
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
isort>=5.12.0

# Logging and monitoring
structlog>=23.0.0

# HTTP requests (backup for web scraping if needed)
requests>=2.31.0
beautifulsoup4>=4.12.0